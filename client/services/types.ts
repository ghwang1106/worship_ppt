export type User = {
  id: number;
  username: string;
  email: string;
  admin: boolean;
  active: boolean;
};

export type Verse = {
  name: string;
  chapter: number;
  verse: number;
}

export type Song = {
  id: string;
  type: string;
}

// export type
